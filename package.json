{
  "name": "rmrec",
  "version": "1.2.1",
  "description": "Runs a native command `rd` or `rf` based on OS by creating & running a child process to delete a folder recursively.",
  "main": "index.js",
  "author": "Alpha Xek @alphaxek",
  "license": "ISC",
  "bin": {
    "rmrec": "./index.js"
  },
  "scripts": {
    "test": "copyfiles ./__tests__/test-data ./__tests__/to-be-deleted-test-data && node ./index.js ./__tests__/to-be-deleted-test-data && jest ./__tests__/index.test.js",
    "build" : "copyfiles ./index.js ./dist/ && copyfiles ./README.md ./dist/"
  },
  "repository": "git://github.com/alphaxek/rmrec.git",
  "devDependencies": {
    "jest": "^29.3.1",
    "copyfiles": "^2.4.1"
  }
}
