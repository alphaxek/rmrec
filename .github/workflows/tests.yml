name: "📝 tests"

on: 
  push:
    branches:
      - 'main'

jobs:
  build:
    steps:
            - name: 📚 checkout
              uses: actions/checkout@v2.5.0
            - name: 🟢 node
              uses: actions/setup-node@v3.5.1
              with:
                  node-version: 18
            - name: 📝 tests
              run: pnpm build

    run-tests:
        needs: build
        steps:
            - name: 📚 checkout
              uses: actions/checkout@v2.5.0
            - name: 🟢 node
              uses: actions/setup-node@v3.5.1
              with:
                  node-version: 18
              env:
                  NODE_AUTH_TOKEN: ${{secrets.PAT}}
            - name: 📦 pnpm
              uses: pnpm/action-setup@v2.2.4
              with:
                version: 7
            - name: 🗂️ copy data
              run: |
                cp -R ./__tests__/test-data ./__tests__/to-be-deleted-test-data
            - name: 📝 tests
              run: pnpm run test-github-actions